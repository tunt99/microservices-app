spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/Shared/projects/microservices-app/config-server-repository
          clone-on-start: true
    fail-fast: true